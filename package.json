{
	"name": "mne-numeric-activity",
	"version": "1.0.2",
	"description": "Les impacts du numérique sur l'environnement. Animation pour les classes de 4ème. Séance 3",
	"author": "alexandretrichot",
	"private": true,
	"scripts": {
		"dev": "nuxt",
		"build": "nuxt build",
		"start": "nuxt start",
		"generate": "nuxt generate",
		"generate:gh-pages": "DEPLOY_ENV=GH_PAGES nuxt generate",
		"deploy:gh-pages": "push-dir --dir=dist --branch=gh-pages --cleanup",
		"update": "npm run generate:gh-pages && npm run deploy:gh-pages"
	},
	"dependencies": {
		"node-sass": "^4.13.0",
		"nuxt": "^2.0.0",
		"sass-loader": "^8.0.2"
	},
	"devDependencies": {
		"push-dir": "^0.4.1"
	}
}
